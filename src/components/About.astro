---
import {
  siteConfig,
  supportedLanguages,
  defaultLanguage,
} from "../config";

const titles = { en: "About Me", de: "Über mich" } as const;
---

<section id="about" class="p-8 sm:p-12 md:p-16 lg:p-24">
  <div>
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-start">
      <div class="lg:col-span-4">
        <h2 class="text-3xl sm:text-4xl md:text-5xl xl:text-7xl font-bold text-gray-900">
          {supportedLanguages.map((lang) => (
            <span
              class={`lang lang-${lang}`}
              data-about-lang={lang}
              hidden={lang !== defaultLanguage}
              key={lang}
            >
              {titles[lang]}
            </span>
          ))}
        </h2>
        <div
          class="w-[75px] h-[5px] mt-2 rounded-full"
          style={`background-color: ${siteConfig.accentColor}`}
        >
        </div>
      </div>

      <div class="lg:col-span-8 space-y-8">
        {supportedLanguages.map((lang) => (
          <p
            class={`text-lg sm:text-xl md:text-2xl leading-relaxed text-gray-600 lang lang-${lang}`}
            data-about-lang={lang}
            hidden={lang !== defaultLanguage}
            key={lang}
          >
            {siteConfig.aboutMe[lang]}
          </p>
        ))}
      </div>
    </div>
  </div>
</section>
